"use client";
import { Suspense } from "react";
import { SubscriptionSuccessPage } from "@views/subscription";
export default function Page() { return <Suspense><SubscriptionSuccessPage /></Suspense>; }
