/**
 * مفاتيح تشغيل المزايا على مستوى الموقع.
 *
 * POINTS_ENABLED: نظام النقاط مخفي بالكامل من الواجهة (طلب العميل) —
 * البيانات تبقى في الباك-إند، لكن لا يظهر رصيد ولا سجل ولا تحويل للمحفظة.
 * حلّ محلّه نظام مستويات العضوية (تصنيف العملاء).
 */
export const POINTS_ENABLED = false;
